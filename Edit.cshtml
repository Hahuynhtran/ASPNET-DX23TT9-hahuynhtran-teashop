@model TeaShopWeb.Models.Product

@{
    ViewBag.Title = "Chỉnh sửa sản phẩm";
}

<div class="container mt-4">
    <h2>✏️ Chỉnh sửa @Model.ProductName</h2>
    <hr />
    @using (Html.BeginForm("Edit", "AdminProduct", FormMethod.Post, new { enctype = "multipart/form-data" }))
    {
        @Html.AntiForgeryToken()
        @Html.ValidationSummary(true)
        @Html.HiddenFor(m => m.Id)
        @Html.HiddenFor(m => m.ImageUrl)

        <div class="row">
            <div class="col-md-8">
                <div class="mb-3">
                    @Html.LabelFor(m => m.ProductName)
                    @Html.TextBoxFor(m => m.ProductName, new { @class = "form-control" })
                </div>
                <div class="mb-3">
                    @Html.LabelFor(m => m.Description)
                    @Html.TextAreaFor(m => m.Description, new { @class = "form-control", rows = 4 })
                </div>
                <div class="row">
                    <div class="col-md-6">
                        @Html.LabelFor(m => m.Price)
                        @Html.TextBoxFor(m => m.Price, new { @class = "form-control", type = "number" })
                    </div>
                    <div class="col-md-6">
                        @Html.LabelFor(m => m.StockQuantity)
                        @Html.TextBoxFor(m => m.StockQuantity, new { @class = "form-control", type = "number" })
                    </div>
                </div>
                <div class="mb-3 mt-3">
                    @Html.LabelFor(m => m.CategoryId)
                    @Html.DropDownListFor(m => m.CategoryId, ViewBag.CategoryList as SelectList, "--- Chọn danh mục ---", new { @class = "form-select" })
                    @Html.ValidationMessageFor(m => m.CategoryId)
                </div>
            </div>
            <div class="col-md-4">
                <div class="mb-3">
                    <label>Ảnh hiện tại</label><br />
                    <img src="@(string.IsNullOrEmpty(Model.ImageUrl) ? "/images/products/default.jpg" : Model.ImageUrl)" width="150" />
                </div>
                <div class="mb-3">
                    <label>Chọn ảnh mới (để trống nếu giữ cũ)</label>
                    <input type="file" name="imageFile" class="form-control" />
                </div>
                <div class="form-check">
                    @Html.CheckBoxFor(m => m.IsActive, new { @class = "form-check-input" })
                    @Html.LabelFor(m => m.IsActive, new { @class = "form-check-label" })
                </div>
            </div>
        </div>
        <button type="submit" class="btn btn-success">Cập nhật</button>
        <a href="@Url.Action("Index")" class="btn btn-secondary">Quay lại</a>
    }
</div>